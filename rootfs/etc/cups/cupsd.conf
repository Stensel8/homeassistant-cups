LogLevel debug2
Listen 0.0.0.0:631
WebInterface Yes
DefaultEncryption Required
ServerCertificate /etc/cups/ssl/server.crt
ServerKey /etc/cups/ssl/server.key
<Location />
  Order allow,deny
  Allow all
</Location>

# Server settings
ServerName *
ServerAlias *
ServerTokens None
MaxJobs 100
MaxJobsPerPrinter 0
MaxJobsPerUser 0

# Security
FileDevice No
Printcap /etc/printcap
PrintcapFormat bsd

# Browse settings
Browsing Off
BrowseLocalProtocols none

## TLS settings are provided by system CUPS/OpenSSL; TLS >=1.2 enforced by certificate/cipher defaults.

# Default policy - ALL WITH ENCRYPTION REQUIRED (add missing operations to silence warnings)
<Policy default>
  JobPrivateAccess default
  JobPrivateValues default
  SubscriptionPrivateAccess default
  SubscriptionPrivateValues default

  <Limit Create-Job Print-Job Print-URI>
    Order deny,allow
    Allow all
    Encryption Required
  </Limit>

  <Limit Send-Document Send-URI Hold-Job Release-Job Restart-Job Purge-Jobs Set-Job-Attributes Create-Job-Subscription Renew-Subscription Cancel-Subscription Get-Notifications Reprocess-Job Cancel-Current-Job Suspend-Current-Job Resume-Job CUPS-Move-Job CUPS-Get-Document>
    Require user @OWNER @SYSTEM
    Order deny,allow
    Allow all
    Encryption Required
  </Limit>

  <Limit CUPS-Add-Modify-Printer CUPS-Delete-Printer CUPS-Add-Modify-Class CUPS-Delete-Class CUPS-Set-Default CUPS-Get-Devices>
    AuthType Basic
    Require user @SYSTEM
    Order deny,allow
    Allow all
    Encryption Required
  </Limit>

  <Limit Pause-Printer Resume-Printer Enable-Printer Disable-Printer Pause-Printer-After-Current-Job Hold-New-Jobs Release-Held-New-Jobs Deactivate-Printer Activate-Printer Restart-Printer Shutdown-Printer Startup-Printer Promote-Job Schedule-Job-After CUPS-Accept-Jobs CUPS-Reject-Jobs>
    AuthType Basic
    Require user @SYSTEM
    Order deny,allow
    Allow all
    Encryption Required
  </Limit>

  <Limit Cancel-Job Cancel-Jobs Cancel-My-Jobs CUPS-Authenticate-Job Validate-Job Close-Job>
    Require user @OWNER @SYSTEM
    Order deny,allow
    Allow all
    Encryption Required
  </Limit>

  <Limit All>
    Order deny,allow
    Allow all
    Encryption Required
  </Limit>
</Policy>
