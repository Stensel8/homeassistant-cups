[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisord.log
pidfile=/var/run/supervisord.pid
loglevel=info

# No managed programs by default; this file remains for compatibility with
# supervisor-based setups.
